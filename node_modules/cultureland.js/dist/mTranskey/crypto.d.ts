import { BigInteger } from "@modern-dev/jsbn";
declare class Crypto {
    sessionKey: number[];
    transkeyUuid: string;
    genSessionKey: string;
    encSessionKey: string;
    allocationIndex: number;
    constructor();
    sha1Hash(msg: string): string;
    tk_f_(s: number, x: number, y: number, z: number): number | undefined;
    ROTL(x: number, n: number): number;
    toHexStr(n: number): string;
    pack(source: string): string;
    xor(a: string, b: string): string;
    mgf1(mgfSeed: string, maskLen: number): string;
    hmacDigest(plainText: string): string;
    rsaes_oaep_encrypt(m: string, n: BigInteger, k: number, e: BigInteger): string;
    phpbb_encrypt2048(plaintext: string, k: number, e: string, n: string): string;
}
export default Crypto;
