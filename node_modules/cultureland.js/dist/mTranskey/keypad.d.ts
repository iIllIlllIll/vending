import { CookieJar } from "tough-cookie";
import Crypto from "./crypto.js";
import { ServletData } from "./types.js";
export default class Keypad {
    servletData: ServletData;
    cookieJar: CookieJar;
    keyboardType: "qwerty" | "number";
    name: string;
    inputName: string;
    fieldType: string;
    crypto: Crypto;
    keyIndex: string;
    constructor(servletData: ServletData, cookieJar: CookieJar, keyboardType: "qwerty" | "number", name: string, inputName: string, fieldType: string, crypto: Crypto);
    /**
     * 비밀번호를 키패드 배열에 따라 암호화합니다.
     * @param pw 비밀번호
     * @param layout 키패드 배열
     * @returns 암호화된 비밀번호
     */
    encryptPassword(pw: string, layout: number[]): string;
    /**
     * 키패드 사진을 분석하여 키패드 배열을 가져옵니다.
     * @returns 키패드 배열
     */
    getKeypadLayout(): Promise<number[]>;
}
